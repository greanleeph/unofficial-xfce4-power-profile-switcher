# xfce4-power-profile-switcher
This is a simple GUI solution for toggling power profiles (via `powerprofilesctl`) in Xfce. 

It utilizes Xfce's built-in application menus to provide a simple GUI toggle for `powerprofilesctl` allowing you to quickly toggle between **Performance**, **Balanced**, and **Power Saver** mode.
